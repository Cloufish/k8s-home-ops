---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: ${pgadmin_app_name}  # This variable is defined inside clusters/production/apps.yaml
  namespace: ${pgadmin_app_name}
spec:
  chart:
    spec:
      version: '>=1.0.0'
  values:
    ingress:
      app:
        hosts:
          - host: ${pgadmin_app_name}.${app_k8s_domain_name}
            paths:
              - path: /
                pathType: Prefix
        tls:
          - hosts: ['${pgadmin_app_name}.${app_k8s_domain_name}']
            secretName: ${pgadmin_app_name}-${default_k8s_cert_name}
