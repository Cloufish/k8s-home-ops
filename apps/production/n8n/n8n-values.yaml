---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: ${n8n_app_name}  # This variable is defined inside clusters/production/apps.yaml
  namespace: ${n8n_app_name}
spec:
  chart:
    spec:
      version: '>=1.0.0'
  values:
    ingress:
      app:
        hosts:
          - host: ${n8n_app_name}.${app_k8s_domain_name}
            paths:
              - path: /
                pathType: Prefix
          - host: ${n8n_app_name}-webhook.${app_k8s_domain_name}
            paths:
              - path: /
                pathType: Prefix
        tls:
          - hosts: ['${n8n_app_name}.${app_k8s_domain_name}']
            secretName: ${n8n_app_name}-${default_k8s_cert_name}
          - hosts: ['${n8n_app_name}-webhook.${app_k8s_domain_name}']
            secretName: ${n8n_app_name}-webhook-${default_k8s_cert_name}
