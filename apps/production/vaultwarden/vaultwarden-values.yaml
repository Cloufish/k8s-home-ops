---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: ${vaultwarden_app_name}  # This variable is defined inside clusters/production/apps.yaml
  namespace: ${vaultwarden_app_name}
spec:
  chart:
    spec:
      version: '>=1.0.0'
  values:
    controllers:
      ${vaultwarden_app_name}:
        replicas: 0
    ingress:
      app:
        hosts:
          - host: ${vaultwarden_app_name}.${app_k8s_domain_name}
            paths:
              - path: /
                pathType: Prefix
        tls:
          - hosts: ['${vaultwarden_app_name}.${app_k8s_domain_name}']
            secretName: wildcard-${default_k8s_cert_name}
