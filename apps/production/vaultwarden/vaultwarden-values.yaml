---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: ${dsomm_app_name}  # This variable is defined inside clusters/production/apps.yaml
  namespace: ${dsomm_app_name}
spec:
  chart:
    spec:
      version: '>=1.0.0'
  values:
    ingress:
      app:
        hosts:
          - host: ${dsomm_app_name}.${app_k8s_domain_name}
            paths:
              - path: /
                pathType: Prefix
        tls:
          - hosts: ['${dsomm_app_name}.${app_k8s_domain_name}']
            secretName: ${dsomm_app_name}-${default_k8s_cert_name}
